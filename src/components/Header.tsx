import React from 'react';

const index: React.FC = () => {
    return (
        <header className="flex bg-primary-gray h-header text-xl font-semibold font-display w-full sticky top-0 z-50 border-b border-gray-300 ">
            <h1 className="m-auto tracking-wider">TripBook</h1>
        </header>
    );
};

export default index;
